import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

import InifiniteScroll from './screens/InfiniteScroll';

export default class App extends React.Component {
    render() {
        return (
            <InifiniteScroll />
        );
    }
}